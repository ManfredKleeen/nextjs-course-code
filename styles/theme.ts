import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
     main: '#0BC2FC'
    },
    secondary: {
      main: '#7FC1FB'
    },
    error: {
      main: '#FF0055'
    },
    background: {
      paper: ''
    }
  }
});

export default theme;